// const add = (a: number = 2, b: number = 1): number => a + b;

// const consoleLogger: (param: number | string) => void = (output) =>
//   console.log(output);

// consoleLogger(add());

// const button = document.querySelector('button');

// if (button) {
//   button.addEventListener('click', (event) => console.log(event));
// }

// const multyAdd = (...items: number[]) =>
//   items.reduce((acc, value) => acc + value, 0);

// console.log(multyAdd(2, 3, 4, 5, 1));

// const incomeObj = {
//   myName: 'Rauf',
//   age: 33,
//   company: 'EPAM',
//   skills: ['JS', 'NODE', 'Express'],
// };

// const { myName, company: newCName } = incomeObj;

// console.log(myName, newCName);
